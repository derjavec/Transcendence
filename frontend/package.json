{
  "name": "frontend",
  "version": "4.1.1",
  "description": "Frontend SPA Pong",
  "scripts": {
    "build:css": "npx tailwindcss -i ./public/styles.css -o ./public/output.css --minify",
    "build:ts": "tsc",
    "build": "npm run build:css && npm run build:ts && cp -r public/* dist/"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0",
    "tailwindcss": "^3.2.0",
    "postcss": "^8.4.16",
    "autoprefixer": "^10.4.8"
  }
}
